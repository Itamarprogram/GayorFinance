<Page x:Class="GayorFinance.LandingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:GayorFinance"
      xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      xmlns:controls="clr-namespace:GayorFinance.Controls"
      mc:Ignorable="d"
      d:DesignHeight="450" d:DesignWidth="800"
      Title="LandingPage">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>


        <ScrollViewer HorizontalScrollBarVisibility="Auto">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="10" >
                <Button Content="Stocks" Style="{StaticResource MaterialDesignRaisedButton}"
                        Margin="5"


                       />
                <Button Content="Portfolios"  Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="5"
                        Click="PortfolioClick"

                   />
                <Button Content="Transactions"  Style="{StaticResource MaterialDesignRaisedButton}"
                    Margin="5"

                   />
            </StackPanel>
        </ScrollViewer>


        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10" HorizontalAlignment="Center">
            <TextBox x:Name="SearchTextBox"
                         materialDesign:HintAssist.Hint="Search"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Width="400" Margin="0,0,10,0"/>
            <Button
               Content="Search"
               Style="{StaticResource MaterialDesignRaisedButton}"
               Click="SearchButton_Click"
               />
        </StackPanel>



    </Grid>
</Page>
